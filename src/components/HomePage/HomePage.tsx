import React, { ReactElement } from 'react';

export const HomePage = (): ReactElement => {
    return (
        <main>
            <h2>sealed.vote</h2>
        </main>
    );
};

export default HomePage;
